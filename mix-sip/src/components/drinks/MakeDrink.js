import React, { Component } from "react"
import { Button } from "semantic-ui-react"

export default class MakeDrink extends Component {
    render(){
        return (
            <div>
                <Button size="mini">Can Mix Now</Button>
            </div>
        )
    }
}